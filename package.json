{
  "name": "jiujitsu-monorepo",
  "private": true,
  "workspaces": [
    "app",
    "backend"
  ],
  "scripts": {
    "app": "npm start --workspace=app",
    "backend": "npm run dev --workspace=backend"
  }
}